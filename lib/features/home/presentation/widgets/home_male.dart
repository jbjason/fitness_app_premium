import 'package:flutter/material.dart';

class HomeMale extends StatelessWidget {
  const HomeMale({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}